/** Largeur max globale — rendu plus spacieux que l’ancien 1280px (réf. maquette). */
export const SITE_MAX_WIDTH_CLASS = 'max-w-[1536px]';

/** Marges horizontales alignées sur la maquette. */
export const SITE_GUTTER_CLASS = 'px-4 sm:px-6 lg:px-10';
